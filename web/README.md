# VINCE web

```text
cd web
npm run dev
```

| Route | Screen |
| --- | --- |
| `/` | Landing |
| `/gate` | Paste a tx · Attestcoin proofs · policy |
| `/desk` | RWA desk — locked until PASS |
| `/markets` | VinceRegistry · RPC latest vs attested |
| `/activity` | Measured Phase 2 prints |

Attestcoin `verifySingle` is a Creditcoin view. Policy + desk settle on Creditcoin Testnet (`npm run deploy:testnet`).
